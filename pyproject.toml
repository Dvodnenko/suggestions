[project]
name = "suggestions"
version = "0.1.0"
description = "recommendation system"
authors = [
    {name = "Dvodnenko",email = "thekovalskii@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "numpy (>=2.5.1,<3.0.0)",
    "pymilvus[milvus-lite] (>=3.0.1,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
